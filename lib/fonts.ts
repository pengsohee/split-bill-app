import { Lexend_Deca } from 'next/font/google';

// Define the font with the desired subsets and weights
export const lexend_deca = Lexend_Deca({
    subsets: ['latin'],
    weight: ['400', '500', '700'],
    display: 'swap',
});
